import { Request, Response, NextFunction } from 'express'; import { ParentEngagementService } from './parent-engagement.service'; const service = new ParentEngagementService(); export class ParentEngagementController { async createUpdate(req: Request, res: Response, next: NextFunction) { try { const update = await service.createUpdate(req.body); res.status(201).json({ success: true, data: update }); } catch (error) { next(error); } } async getUpdates(req: Request, res: Response, next: NextFunction) { try { const updates = await service.getUpdates(req.params.parentId); res.json({ success: true, data: updates }); } catch (error) { next(error); } } }
