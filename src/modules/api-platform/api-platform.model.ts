import mongoose, { Schema } from 'mongoose'; import { IApiKey } from './api-platform.interface'; const schema = new Schema<IApiKey>({ userId: { type: String, required: true, index: true }, key: { type: String, required: true, unique: true }, name: String, permissions: [String], status: { type: String, enum: ['active', 'revoked'], default: 'active' } }, { timestamps: true }); export const ApiKey = mongoose.model<IApiKey>('ApiKey', schema);
