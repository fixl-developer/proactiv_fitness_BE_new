import mongoose, { Schema } from 'mongoose'; import { IWearableData } from './wearables.interface'; const schema = new Schema<IWearableData>({ userId: { type: String, required: true, index: true }, deviceType: String, heartRate: Number, steps: Number, calories: Number, syncedAt: Date }, { timestamps: true }); export const WearableData = mongoose.model<IWearableData>('WearableData', schema);
