import { Request, Response, NextFunction } from 'express'; import { ReferralService } from './referral.service'; const service = new ReferralService(); export class ReferralController { async createReferral(req: Request, res: Response, next: NextFunction) { try { const referral = await service.createReferral(req.body); res.status(201).json({ success: true, data: referral }); } catch (error) { next(error); } } async getReferrals(req: Request, res: Response, next: NextFunction) { try { const referrals = await service.getReferrals(req.params.referrerId); res.json({ success: true, data: referrals }); } catch (error) { next(error); } } }
